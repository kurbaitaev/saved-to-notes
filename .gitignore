# Secrets — never commit
.env

# Personal data: your processed reels, your notes, your logs.
# These are yours; they don't belong in the repo.
ledger.json
ledger.corrupt-*.json
pending.json
logs/
vault/
tmp/

# Installed launchd files (generated from launchd/*.template by install.sh)
launchd/*.plist

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# macOS
.DS_Store

# Cross-process lock file (created at runtime)
.ledger.lock
